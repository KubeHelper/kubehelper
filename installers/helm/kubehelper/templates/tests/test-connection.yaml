apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubehelper.fullname" . }}-test-connection"
  labels:
{{ include "kubehelper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubehelper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
